CREATE TABLE game_events (
    id TEXT,
    game_str TEXT,
    play_id INTEGER,
    at_bat TEXT,
    play_per_game INTEGER,
    timestamp INTEGER,
    player_position INTEGER,
    event_code INTEGER
);
